#include "../../inc/minishell.h"

int ft_export(char **envp, t_cmd *cmd)
{
    int i;
    int j;

    i = 1;
    j = 0;

    // while (cmd->args[i])
    // while (envp[j])
    //     j++;
    //ft_putstr_fd(&envp[j][i], 1);
    // printf("fdsfs\n");
    printf("%c\n", envp[0][0]);
    return (1);
    if (cmd)
    {
        
    }
}